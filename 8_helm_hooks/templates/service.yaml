apiVersion: v1
kind: Service
metadata:
  name: {{ include "8_helm_hooks.fullname" . }}
  labels:
    {{- include "8_helm_hooks.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "8_helm_hooks.selectorLabels" . | nindent 4 }}
