apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "8_helm_hooks.fullname" . }}-test-connection"
  labels:
    {{- include "8_helm_hooks.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "8_helm_hooks.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
